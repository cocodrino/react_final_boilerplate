import React from 'react'
import styled from "styled-components/macro"

export const SSampleBox = styled.div({
    padding:10,
    maxWidth: "80%",
    margin: "auto",
    marginTop:"30px",
    backgroundColor: "beige"
})